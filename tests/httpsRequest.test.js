import {
    describe,
    it
} from "mocha";
import {
    expect
} from "chai";

import httpsRequest, {
    request,
    options,
    get,
    head,
    post,
    put,
    patch,
    del
} from "../src/httpsRequest";

describe("httpsRequest tests", () => {

    it("This file was read", () => {
        expect(1).to.be.equal(1);
    });


});